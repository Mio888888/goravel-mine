apiVersion: v1
kind: Service
metadata:
  name: {{ include "goravel-mine.fullname" . }}
  labels:
    {{- include "goravel-mine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "goravel-mine.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
